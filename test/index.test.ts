import { render } from 'vactapp'


/*test('function: render(type, props, children): VNode', () => {
  expect(render('span', null, [])).toEqual({
    type: 'span',
    flag: 2,
    props: {},
    children: []
  });
})*/
